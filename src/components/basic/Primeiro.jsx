import React from 'react'

export default function Primeiro(){
const msg = 'Seja Bem-vindo(a)'
  return(
    <div>
      <h2>
        primeiro componente
      </h2>
      <p>{msg}</p>
    </div>
  )}
